apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-project-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-project
  template:
    metadata:
      labels:
        app: devops-project
    spec:
      containers:
      - name: python-app
        image: docker_user_placeholder/devops-final-project:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000